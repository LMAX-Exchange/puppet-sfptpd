fixtures:
  repositories:
    stdlib:
      repo: 'https://github.com/puppetlabs/puppetlabs-stdlib.git'
    logrotate:
      repo: 'https://github.com/voxpupuli/puppet-logrotate.git'
    concat:
      repo: 'https://github.com/puppetlabs/puppet-concat.git'
  symlinks:
    sfptpd: "#{source_dir}"
